import Vue from 'vue'

import {
  Button, Cell, CellGroup, Card, Icon, Popup, NavBar,
  Field, Form, Toast, CountDown
} from 'vant'

Vue.use(Button)
  .use(Cell).use(CellGroup).use(Card).use(Popup).use(Icon)
  .use(NavBar).use(Field).use(Form).use(Toast).use(CountDown)
